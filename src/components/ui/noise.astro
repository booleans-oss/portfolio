---
---

<svg
    class="pointer-events-none fixed isolate z-50 opacity-70 mix-blend-soft-light"
    width="100%"
    height="100%"
>
    <svg class="absolute top-0 left-0">
        <filter id="clementthelegend">
            <feTurbulence
                type="fractalNoise"
                baseFrequency="0.6"
                stitchTiles="stitch"></feTurbulence>
        </filter>
    </svg>
    <rect width="100%" height="100%" filter="url(#clementthelegend)"></rect>
</svg>
